#include <stdio.h>
/**
 * main - main function
 * Return : nothing
 */
int main(void)
{
       char alpha = 'a';
	
       while (alpha <='z')
       {
		putchar(alpha);
		alpha++;
       }
       putchar('\n');
return (0);
}
